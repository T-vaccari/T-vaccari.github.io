name: Jekyll Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ruby:3.1
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Jekyll
        run: gem install jekyll
      - name: Build site
        run: jekyll build
